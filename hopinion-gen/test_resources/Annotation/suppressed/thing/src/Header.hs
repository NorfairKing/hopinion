-- TODO a marker above the module header, which is about the file.
module Header where

-- [allow:file:CommentBareTodo] The marker above the module header attaches to
-- the file rather than to any declaration, so the suppression for it has to
-- reach the file. A site-scoped one here would be attached to nothing.

value :: Int
value = 1
